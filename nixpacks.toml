[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm install",
  "npm run build",
  "cd server && npm install",
  "cd server && npx prisma generate"
]

[start]
cmd = "cd server && npm start"
