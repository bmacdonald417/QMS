/**
 * Client-side typing helpers for workflow templates.
 * Runtime definitions are served from `GET /api/org/:slug/qms-agent/workflow-templates`.
 */
export type { WorkflowTemplateDefinition } from '../contracts';
