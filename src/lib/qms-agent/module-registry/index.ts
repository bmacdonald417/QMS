/**
 * Client-side re-exports for QMS module registry typing.
 * Runtime definitions are served from `GET /api/org/:slug/qms-agent/modules`.
 */
export type { QmsModuleDefinition } from '../contracts';
