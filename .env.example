# Optional: set when the frontend is built (e.g. npm run preview) or deployed and the API runs elsewhere.
# In dev with npm run dev, the Vite proxy forwards /api to localhost:3001, so this can be left unset.
# VITE_API_URL=http://localhost:3001
